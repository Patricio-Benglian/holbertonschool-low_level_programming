#include "main.h"
/**
 * _abs - computes the absolute value of an int
 * @int:  integer
 * Return: Always 0 (Success)
 */
int _abs(int)
{
	if (int < 0)
	{
		int = int * -1;
	}
	else
	{
		int = int;
	}
	return (0);
}
