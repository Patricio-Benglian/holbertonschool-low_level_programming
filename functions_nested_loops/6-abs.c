#include "main.h"
/**
 * _abs - computes the absolute value of an int
 * @r: integer to computer
 * Return: Always 0 (Success)
 */
int _abs(int r)
{
	if (r < 0)
	{
		r = r * -1;
	}
	else
	{
		r = r;
	}
	return (0);
}
