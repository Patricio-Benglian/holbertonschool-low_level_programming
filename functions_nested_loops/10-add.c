#include "main.h"
/**
 * add - adds both integers
 * @add1: first integer to add
 * @add2: second integer to add
 * Return: Always sum of both integers (Success)
 */
int add(int add1, int add2)
{
	return (add1 + add2);
}
