#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include "3-calc.h"

/**
 * main - TBD
 *
 * Return: Always 0 (Success)
 */
int main(argc, **argv)
{
	/* if there arent 4 args then the command was run incorrectly, error */
	if (argc != 4)
	{	printf("Error\n");
		exit(98);
	}


}
