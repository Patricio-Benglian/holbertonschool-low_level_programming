#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	if (n < 0)
	{
		printf("n is negative");
	}
		else 
			if (n > 0)
			{
				printf("n is positive);
			}
			else
			{
				printf("n is zero");
			}
}
